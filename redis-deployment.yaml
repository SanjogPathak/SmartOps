apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartops-redis
  namespace: smartops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smartops-redis
  template:
    metadata:
      labels:
        app: smartops-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: smartops-redis
  namespace: smartops
spec:
  selector:
    app: smartops-redis
  ports:
    - port: 6379
      targetPort: 6379
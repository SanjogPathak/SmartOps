apiVersion: v1
kind: Service
metadata:
  name: smartops-api
  namespace: smartops
spec:
  selector:
    app: smartops-api
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080